'use strict';

/**
 * @ngdoc function
 * @name primeiroApp.controller:TesteCtrl
 * @description
 * # TesteCtrl
 * Controller of the primeiroApp
 */
angular.module('primeiroApp')
  .controller('TesteCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
